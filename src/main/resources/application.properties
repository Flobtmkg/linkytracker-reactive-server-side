# Profile "test" add H2 console server and additional testing features
spring.profiles.active=test

# DB
# Change database location here
spring.r2dbc.url=r2dbc:h2:file:///C:/Users/Flo/Documents/BDD/dataserver;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
# Change database auth here
spring.r2dbc.username=admin
spring.r2dbc.password=password

# SQL logs
#logging.level.org.springframework.r2dbc=TRACE

# DB H2 console
h2-server.port=8082

#sql init
spring.sql.init.mode=always

# Sometimes at the first init no data is shown in the react app even if data is present, therefore a server reset is required
# May be this param can help if some JPA beans init before the execution of the init scripts (schema and data) ?? 
spring.main.lazy-initialization=true


# Web APIs
server.port=8080
server.http2.enabled=true
allow-origins=http://localhost:19006